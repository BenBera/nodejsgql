let Joi;_161‍.x([["loginValidate",()=>loginValidate],["regesterValidate",()=>regesterValidate]]);_161‍.w("@hapi/joi",[["default",["Joi"],function(v){Joi=v}]]);
const name = Joi.string().max(255).required().label("Name");
const email = Joi.string().email().required().label("Email");
const username = Joi.string().max(255).min(6).required().label("Username");
const password = Joi.string()
  .max(30)
  .min(8)
  .required()
  .regex(/^(?=\s*[a-z])(?=s\*[A-Z])(?=\s*\d).*$/)
  .label("Password").message({
      "string.regex":  "Must  have  atleast one lowercase letter, one uppercase letter and one digit"
  })
//   options({
//       language: {
//           string:{
//               regex:{
//                   base: "Must  have  atleast one lowercase letter, one uppercase letter and one digit"
//               }
//           }
//       }
//   });
       const loginValidate = Joi.object().keys({
    username,
    password
});
       const regesterValidate = Joi.object().keys({
    name,
    email,                                                                                                      
    username,
    password
})