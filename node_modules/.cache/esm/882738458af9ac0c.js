let jwt,APP_SECRET,APP_REFRESH_SECRET;_8af‍.x([["issueTokens",()=>issueTokens]]);_8af‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);_8af‍.w("../config",[["APP_SECRET",["APP_SECRET"],function(v){APP_SECRET=v}],["APP_REFRESH_SECRET",["APP_REFRESH_SECRET"],function(v){APP_REFRESH_SECRET=v}]]);

       const issueTokens = async ({ username, email, name, id }) => {
  let token = await jwt.sign({ username, email, name }, APP_SECRET, {
    expiresIn: 120,
  });
  let refreshToken = await jwt.sign(
    { username, email, name },
    APP_REFRESH_SECRET,
    { expiresIn: "2d" }
  );
  return {
      token,
      refreshToken
  }
};
